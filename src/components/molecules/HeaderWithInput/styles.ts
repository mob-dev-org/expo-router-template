import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    inputContainer: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    container: {
        backgroundColor: '#fff',
        paddingHorizontal: 16,
    },
    input: {
        paddingLeft: 8,
    },
});
