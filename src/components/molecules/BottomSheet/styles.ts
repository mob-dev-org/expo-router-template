import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    bottomSheet: {
        elevation: 5,
        shadowOffset: { width: 0, height: 0 },
        shadowOpacity: 0.1,
        shadowRadius: 5,
    },
});
